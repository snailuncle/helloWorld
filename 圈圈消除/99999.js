// var obj = {'0':'a','1':'b','2':'c'};

// Object.keys(obj).forEach(function(key){

//      console.log(key,obj[key]);

// })
var arr = [1, 2, 3, 4];

arr.forEach(function (index) {

    console.log(index);
});
